<div class="container mx-auto px-4 py-8">
  <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between timeline-header">
    <div>
      <h1 class="page-title mb-0">Timeline</h1>
      <p class="text-secondary mb-0">Latest reviews from your mates</p>
    </div>
  </div>

  <% if @mates_reviews.any? %>
    <div id="timeline-reviews" class="space-y-4">
      <%= render "users/timeline_reviews", reviews: @mates_reviews, page: @page, year: current_year %>
    </div>
  <% else %>
    <p class="text-center text-sm text-slate-500">No reviews from your mates yet.</p>
  <% end %>

  <%# Back to Top %>
  <button onclick="window.scrollTo({top: 0, behavior: 'smooth'})" class="back-to-top" title="Back to top">
    <i data-lucide="arrow-up" style="color: var(--text-tertiary);"></i>
  </button>
</div>
