<%# Mate Timeline Item - Shows a mate's review with poster and details %>
<%# Local variables: review %>

<div class="timeline-item flex flex-row gap-3 sm:gap-4 rounded-lg border border-slate-200 bg-white p-3 sm:p-4">
  <%# Left: Movie Poster %>
  <div class="flex-shrink-0 relative">
    <% my_review = current_user&.reviews&.find_by(movie: review.movie) %>

    <%# My rating badge - clickable to edit review %>
    <% if my_review&.stars.present? %>
      <%= link_to edit_movie_review_path(review.movie, my_review, year: current_year, return_to: request.fullpath),
          class: "movie-card-badge animate-float",
          data: { turbo: false } do %>
        <span class="rating-number"><%= my_review.stars %></span>
        <i data-lucide="edit-2" class="edit-icon"></i>
      <% end %>
    <% end %>

    <% if review.movie.picture_url.present? %>
      <%= image_tag review.movie.picture_url,
                    alt: review.movie.title,
                    class: "w-24 h-36 sm:w-[100px] sm:h-[150px] md:w-[133px] md:h-[200px] rounded-lg object-cover shadow-sm" %>
    <% else %>
      <div class="w-24 h-36 sm:w-[100px] sm:h-[150px] md:w-[133px] md:h-[200px] flex items-center justify-center rounded-lg bg-slate-100 shadow-sm">
        <i data-lucide="film" class="text-slate-400" style="width: 48px; height: 48px;"></i>
      </div>
    <% end %>
  </div>

  <%# Right: Content Area %>
  <div class="flex flex-1 flex-col gap-2">
    <%# Top: User Info with Avatar and Rating Badge %>
    <div class="timeline-content flex items-start gap-2">
      <div class="flex items-center gap-2 flex-1 min-w-0 timeline-user-info">
        <%= render "shared/avatar", user: review.user, size: :md %>
        <div class="timeline-text flex-1 min-w-0">
          <div class="text-xl font-semibold text-slate-900">
            <%= link_to review.user.name, user_path(review.user), class: "timeline-username-link" %>
          </div>
          <div class="text-lg italic text-slate-600 timeline-event">
            Watched "<%= link_to review.movie.title, movie_path(review.movie, year: current_year, back_to: request.fullpath), class: "timeline-movie-title" %>"
            <% days_ago = (Date.current - review.watched_on.to_date).to_i %>
            <span title="<%= review.watched_on.strftime('%B %d, %Y at %I:%M %p') %>">
              <% if days_ago == 0 %>
                Today
              <% elsif days_ago == 1 %>
                Yesterday
              <% else %>
                <%= days_ago %> days ago
              <% end %>
            </span>
          </div>

          <%# Mate Rating - stars + x/10 (mobile/tablet - below event) %>
          <div class="timeline-rating-mobile lg:hidden mt-1">
            <%= render "shared/mate_rating", rating: review.stars, size: :md %>
          </div>
        </div>
      </div>

      <%# Mate Rating - stars + x/10 (desktop - right side) %>
      <div class="timeline-rating-desktop hidden lg:block flex-shrink-0">
        <%= render "shared/mate_rating", rating: review.stars, size: :lg %>
      </div>
    </div>

    <%# Comment Section - full comments without truncation %>
    <% if review.comment.present? %>
      <div class="mt-2 rounded-md bg-slate-50 p-3 text-sm text-slate-700">
        <%= review.comment %>
      </div>
    <% end %>
  </div>
</div>
