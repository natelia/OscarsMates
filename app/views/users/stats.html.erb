<div class="container mx-auto px-4 py-8">
  <div class="flex flex-col gap-4 md:flex-row md:items-center md:justify-between stats-header">
    <div>
      <h1 class="page-title mb-0">Your Stats</h1>
      <p class="text-secondary mb-0">Movies Watched: <strong><%= @total_movies_watched %></strong> • Total Minutes: <strong><%= @total_minutes_watched %> mins</strong></p>
    </div>
  </div>

  <div data-controller="chart"
       data-chart-minutes-watched-value="<%= @mates_stats.to_json %>">
    <canvas></canvas>
  </div>
</div>
