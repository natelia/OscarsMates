<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Reviews for <%= @movie.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #ffffff;
      color: #333333;
    }

    .container {
      max-width: 1000px;
      margin: 0 auto;
      padding: 10px;
    }

    .info {
      margin-top: 10px;
    }

    .info a {
      text-decoration: none;
      color: #555555;
    }

    .info small {
      color: #999999;
    }

    .btn-container {
      margin-top: 20px;
      text-align: center;
    }
  </style>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    </div>
    <h3 class="text-center mt-4">Reviews for <%= link_to @movie.title, @movie, style: "text-decoration: none; color: #555555;" %></h3>
    <div class="btn-container">
      <%= link_to "Home Page", root_path, class: "btn btn-outline-dark" %>
      <%= link_to "Movie Details", @movie, class: "btn btn-outline-dark" %>
    </div>
    <ul class="reviews list-group mt-4">
      <% @reviews.reverse_each do |review| %>
        <li class="list-group-item">
          <div class="review">
            <div class="stars text-center">
              <%= render_stars(review.stars) %>
            </div>
            <div class="info text-center">
              <p>
                <%= link_to review.user.name, review.user, style: "text-decoration: none; color: #555555;" %><br>
                <small><%= time_ago_in_words(review.created_at) %> ago</small>
              </p>
              <p class="comment">
                <%= review.comment %>
              </p>
            </div>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</body>
</html>
